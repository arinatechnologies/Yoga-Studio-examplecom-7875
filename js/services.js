// Optional: Add smooth scrolling to service details when implemented
document.querySelectorAll('.learn-more-btn').forEach(button => {
  button.addEventListener('click', (e) => {
    e.preventDefault();
    // This can be expanded to show service details modal or scroll to section
    console.log('Learn more clicked - service specific action would go here');
  });
});